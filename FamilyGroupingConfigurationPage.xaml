<Page x:Class="SmtLineAllocationUI.FamilyGroupingConfigurationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Family groupings">
  <Grid Background="#F4F6FA"
        Margin="0">
    <Border Background="White"
            CornerRadius="8"
            Padding="20"
            Margin="0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="0,0,0,8">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0"
                      Orientation="Horizontal">
            <Button x:Name="BtnBack"
                    Content="Back"
                    Width="110"
                    Height="36"
                    Margin="0,0,8,0"
                    Click="BtnBack_OnClick" />
            <Button x:Name="BtnSave"
                    Content="Save"
                    Width="100"
                    Height="36"
                    Margin="0,0,8,0"
                    Click="BtnSave_OnClick" />
            <Button x:Name="BtnAddRow"
                    Content="Add row"
                    Width="100"
                    Height="36"
                    Margin="0,0,8,0"
                    Click="BtnAddRow_OnClick" />
            <Button x:Name="BtnDeleteRow"
                    Content="Delete row"
                    Width="110"
                    Height="36"
                    Click="BtnDeleteRow_OnClick" />
          </StackPanel>

          <TextBlock Grid.Column="1"
                     Text="FAMILY GROUPINGS"
                     FontSize="20"
                     FontWeight="Bold"
                     Foreground="#333333"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Center"
                     TextAlignment="Center" />
        </Grid>

        <TextBlock x:Name="TxtSubtitle"
                   Grid.Row="1"
                   Foreground="#777777"
                   FontSize="12"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap" />

        <DataGrid x:Name="GridData"
                  Grid.Row="2"
                  AutoGenerateColumns="True"
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  EnableRowVirtualization="True"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True" />
      </Grid>
    </Border>
  </Grid>
</Page>
