<Page x:Class="SmtLineAllocationUI.SmtLineConfigurationMatrixPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="SMT LINE CONFIGURATION">
  <Grid Background="#F4F6FA"
        Margin="0">
    <Border Background="White"
            CornerRadius="8"
            Padding="20"
            Margin="0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="0,0,0,16">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <StackPanel Grid.Column="0"
                      Orientation="Horizontal">
            <Button x:Name="BtnBack"
                    Content="Back"
                    Width="110"
                    Height="36"
                    Margin="0,0,8,0"
                    Click="BtnBack_OnClick" />
            <Button x:Name="BtnSaveMatrix"
                    Content="Save"
                    Width="100"
                    Height="36"
                    Margin="0,0,8,0"
                    Click="BtnSaveMatrix_OnClick" />
            <Button x:Name="BtnAddMatrixRow"
                    Content="Add row"
                    Width="100"
                    Height="36"
                    Margin="0,0,8,0"
                    Click="BtnAddMatrixRow_OnClick" />
            <Button x:Name="BtnDeleteMatrixRow"
                    Content="Delete row"
                    Width="110"
                    Height="36"
                    Click="BtnDeleteMatrixRow_OnClick" />
          </StackPanel>

          <TextBlock Grid.Column="1"
                     Text="SMT LINE CONFIGURATION"
                     FontSize="20"
                     FontWeight="Bold"
                     Foreground="#333333"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Center"
                     TextAlignment="Center" />
        </Grid>

        <DataGrid x:Name="GridData"
                  Grid.Row="1"
                  AutoGenerateColumns="True"
                  IsReadOnly="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  ColumnWidth="*"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto" />
      </Grid>
    </Border>
  </Grid>
</Page>
